<div class="title-bar">
  <h1 class="flex-item">Liste des événements</h1>
  <% if policy(@events).new? %>
    <%= link_to "Ajouter un spectacle", new_event_path, class: "btn btn-ghost flex-item" %>
  <% end %>
</div>
<ul>
  <%= render "cards" %>
</ul>
