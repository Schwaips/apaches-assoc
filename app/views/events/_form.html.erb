<%= simple_form_for @event do |f| %>
  <div class="form-inputs">
    <%= f.input :title, label: "Nom", placeholder: "L'avare" %>
    <%= f.input :duration, label: "Durée" %>
    <%#= f.association :actor, collection: Actor::name %>
    <%#= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn-flat-primary' %>
  </div>
<% end %>

